{
  "name": "images-to-pdf",
  "version": "1.0.0",
  "type": "module",
  "description": "Browser-based PDF processing tool with privacy-first approach",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vitest": "^2.0.0"
  }
}
